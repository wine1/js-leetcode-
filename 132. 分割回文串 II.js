// https://leetcode-cn.com/problems/palindrome-partitioning-ii/
/**
 * @param {string} s
 * @return {number}
 */
//  给你一个字符串 s，请你将 s 分割成一些子串，使每个子串都是回文。

//  返回符合要求的 最少分割次数 。
 var minCut = function(s) {
    let res=[]
    function dfs(temp,n) {
        if(s.length===n){
            res.push(temp)
        }else{
            
        }
    }

    dfs[0]
};

console.log(minCut('aabba'))